# Rentcar App 🚗

**Каталог оренди авто на Next.js + TypeScript**

---

## Огляд

Rentcar App — це демонстраційний застосунок для перегляду та оренди автомобілів.  
Він підтримує:

- Каталог авто з пагінацією та кнопкою **Load More**
- Фільтри за брендом, ціною за годину та пробігом
- Пошук із застосуванням всіх активних фільтрів одночасно
- Додавання авто в обране (стан зберігається через Zustand)
- Перегляд деталей авто з фото та формою бронювання

Дані отримуються з **публічного API**: [Car Rental API](https://car-rental-api.goit.global/api-docs/)

---

## Технології ⚒️

- **Next.js** (App Router) + **React** + **TypeScript**
- **Axios** для HTTP-запитів
- **Zustand** для глобального стану (обране)
- **CSS Modules** для стилів
- Валідація вводу та безпечні перетворення даних

---

## Структура проєкту

- `app/` — Next.js App Router
  - `catalog/` — каталог та сторінка деталей авто
    - `page.tsx` — серверна частина каталогу
    - `CatalogClient.tsx` — клієнтська частина каталогу
    - `[id]/page.tsx` — сторінка конкретного авто
  - `components/` — UI-компоненти (FilterBar, CarDetails тощо)
  - `services/api.ts` — функції для роботи з API
  - `store/useCarsStore.ts` — Zustand store для обраного

---

## Можливості 🚀

### Каталог

- Перегляд списку авто
- Пагінація на бекенді
- Кнопка **Load More** для підвантаження наступних авто

### Фільтри

- Бренд (All brands очищає фільтр)
- Ціна за годину
- Пробіг (from/to, підтримка коми та крапки)

### Пошук

- Кнопка **Search** застосовує всі активні фільтри та оновлює URL-параметри (`brand`, `price`, `mileageFrom`, `mileageTo`)

### Обране

- Додавання/видалення авто у **favorites** через Zustand

### Деталі авто

- Фото, повна інформація та форма бронювання

---

## Маршрути ⛗

| Маршрут         | Опис                                                      |
| --------------- | --------------------------------------------------------- |
| `/`             | Домашня сторінка з Hero та переходом до каталогу          |
| `/catalog`      | Перелік авто з фільтрами та пагінацією                    |
| `/catalog/[id]` | Сторінка конкретного авто з деталями та формою бронювання |

---

## Встановлення та запуск 💻

1. Клонувати репозиторій:

```bash
git clone <repo_url>
cd "Rentcar application/rentcar-app"
```
